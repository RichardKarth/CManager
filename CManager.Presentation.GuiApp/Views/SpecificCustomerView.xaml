<UserControl x:Class="CManager.Presentation.GuiApp.Views.SpecificCustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CManager.Presentation.GuiApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="560" d:DesignWidth="400"
             
             
             
             xmlns:models="clr-namespace:CManager.Domain.Models;assembly=CManager.Domain" 
             xmlns:viewModels="clr-namespace:CManager.Presentation.GuiApp.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:SpecificCustomerViewModel}"
             
             
             >
             <!--Fick hjälp av chattgpt att skriva models sökvägen, det där med assembly osv-->
    <Grid>

        <Border Background="#2A2A2A" CornerRadius="25">
            <Grid>

                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>

                <StackPanel>
                    <TextBlock Grid.Row="0" Text="{Binding Title, FallbackValue='Search Customer'}" Style="{StaticResource TitleText}" HorizontalAlignment="Center" Margin="0 30"/>
                </StackPanel>

                <Grid Grid.Row="1">

                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Margin="20 0" Grid.Row="0">
                        <TextBox Text="{Binding SearchEmail, UpdateSourceTrigger=PropertyChanged}" />

                                
                    </StackPanel>
                    <Button Grid.Row="1" Command="{Binding GetCustomerByEmailCommand}"
                        CommandParameter="{Binding SearchEmail}"
                        Width="200"
                        Height="30"
                        HorizontalAlignment="Center" Margin="0 12"
                        Content="Search">
                    </Button>
                </Grid>




                <ContentControl Grid.Row="2" Content="{Binding Customer}">
                    <ContentControl.ContentTemplate>
                        <DataTemplate>
                            <Border Background="White" CornerRadius="25" Margin="20 0">
                                <StackPanel Margin="20">
                                    <TextBlock Text="{Binding Id}" />
                                    <TextBlock Text="{Binding FirstName}" />
                                    <TextBlock Text="{Binding LastName}" />
                                    <TextBlock Text="{Binding Email}" />
                                    <TextBlock Text="{Binding PhoneNumber}" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ContentControl.ContentTemplate>
                </ContentControl>

                <Button Grid.Row="3" Style="{StaticResource CancelButton}" Content="Go Back" Margin="20" Command="{Binding BackToMenuViewCommand}" ></Button>

            </Grid>
        </Border>

    </Grid>
</UserControl>
