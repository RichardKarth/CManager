<UserControl x:Class="CManager.Presentation.GuiApp.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CManager.Presentation.GuiApp.Views"
             mc:Ignorable="d" 
             
             
             
             xmlns:viewModels="clr-namespace:CManager.Presentation.GuiApp.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:MenuViewModel}"
            
             d:DesignHeight="560" d:DesignWidth="400"
             >

    <Border Background="#2A2A2A" CornerRadius="25">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                
                
                
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
                <TextBlock Text="{Binding Title, FallbackValue='CManager Menu'}" Style="{StaticResource TitleText}"/>
            </StackPanel>


            <Button Grid.Row="1" Content="Add Customer" Style="{StaticResource StandardButton}" Width="170" Height="40" Margin="0,0,0,10" Command="{Binding OpenAddCustomerViewCommand}"/>
            <Button Grid.Row="2" Content="View Customers" Style="{StaticResource StandardButton}"  Width="170" Height="40" Margin="0,0,0,10" Command="{Binding OpenViewCustomersViewCommand}"/>
            <Button Grid.Row="3" Content="Delete Customer" Style="{StaticResource StandardButton}"  Width="170" Height="40" Margin="0,0,0,10" Command="{Binding OpenDeleteCustomerViewCommand}"/>

        </Grid>

    </Border>

</UserControl>
