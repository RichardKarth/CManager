<UserControl x:Class="CManager.Presentation.GuiApp.Views.ViewCustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CManager.Presentation.GuiApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="560" d:DesignWidth="400"
             
             xmlns:viewModels="clr-namespace:CManager.Presentation.GuiApp.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:ViewCustomerViewModel}"
             
                
       
             
             >

    <Border Background="#2A2A2A" CornerRadius="25">
        <Grid>
        
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <StackPanel>
                <TextBlock Grid.Row="0" Text="{Binding Title, FallbackValue='Customer List'}" Style="{StaticResource TitleText}" HorizontalAlignment="Center" Margin="0 30"/>
            </StackPanel>


            <ListView Grid.Row="1" ItemsSource="{Binding Customers}" Margin="20 10" HorizontalContentAlignment="Stretch">
                <ListView.ItemTemplate>
                    
                    <DataTemplate DataType="models:Customer">
                        <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="10" Padding="10" Margin="0 5">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>


                                <StackPanel Grid.Column="0" HorizontalAlignment="right" VerticalAlignment="Center"  >
                                    <TextBlock Text="{Binding FirstName}" FontWeight="Bold" FontSize="16"/>
                                    <TextBlock Text="{Binding LastName}" FontWeight="Bold" FontSize="16"/>
                                    <TextBlock Text="{Binding Email}" FontSize="14"/>
                                    <TextBlock Text="{Binding PhoneNumber}" FontSize="14"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                    <Button Content="Edit" Width="60" Height="25" Margin="0 0 0 5" Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding .}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Button Grid.Row="2" Style="{StaticResource CancelButton}" Content="Go Back" Margin="20" Command="{Binding BackToMenuViewCommand}" ></Button>
            
        </Grid>
    </Border>
</UserControl>
