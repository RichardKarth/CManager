<Window x:Class="CManager.Presentation.GuiApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CManager.Presentation.GuiApp"
        mc:Ignorable="d"
        
        Background="Transparent"
      
        xmlns:views="clr-namespace:CManager.Presentation.GuiApp.Views"
        xmlns:models="clr-namespace:CManager.Domain.Models;assembly=CManager.Domain"
        
        xmlns:viewModels="clr-namespace:CManager.Presentation.GuiApp.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewModels:MainViewModel}"
        
        
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        
        Height="600"
        Width="400"
        Title="MainWindow"
        >

    <Border Background="#1E1E1E" CornerRadius="25">
        <Grid Margin = "20">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <Grid Grid.Row="0" x:Name="TopBar" MouseDown="TopBar_MouseDown" Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="1">
                    <Button Style="{StaticResource ExitButton}" x:Name="ExitButton" Click="ExitButton_Click"    ></Button>
                </Grid>
            </Grid>
            
            <ContentControl Content="{Binding CurrentViewModel}" Grid.Row="1">
                <ContentControl.Resources>

                    <DataTemplate DataType="{x:Type viewModels:AddCustomerViewModel}">
                        <views:AddCustomerView/>
                    </DataTemplate>

                    <DataTemplate DataType="{x:Type viewModels:MenuViewModel}">
                        <views:MenuView/>
                    </DataTemplate>

                    <DataTemplate DataType="{x:Type viewModels:ViewCustomerViewModel}">
                        <views:ViewCustomerView/>
                    </DataTemplate>

                    <DataTemplate DataType="{x:Type viewModels:EditCustomerViewModel}">
                        <views:EditCustomerView/>
                    </DataTemplate>

                    <DataTemplate DataType="{x:Type viewModels:DeleteCustomerViewModel}">
                        <views:DeleteCustomerView/>
                    </DataTemplate>

                    <DataTemplate DataType="{x:Type viewModels:SpecificCustomerViewModel}">
                        <views:SpecificCustomerView/>
                    </DataTemplate>

                </ContentControl.Resources>
            </ContentControl>
        </Grid>

    </Border>
    
</Window>
